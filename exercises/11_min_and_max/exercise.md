# Min and Max

Given an array of numbers in `input.json`, write a `jq` program that builds an
object holding the minimum and maximum values (under the keys `min` and `max`)
and writes it to `output.json`.
