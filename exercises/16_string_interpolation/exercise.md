# String Interpolation

Given an array of objects with `first` and `last` fields in `input.json`, write
a `jq` program that builds a full-name string (`"first last"`) for each person
and writes the resulting array of strings to `output.json`.
