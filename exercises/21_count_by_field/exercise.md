# Count by Field

Given an array of person objects in `input.json`, write a `jq` program that
counts how many people are in each `dept` and writes an array of
`{dept, count}` objects to `output.json`.
