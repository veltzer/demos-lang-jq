# Extract Value of Object Key

Given an object in `input.json`, write a `jq` program that extracts the value of
the `fruit` key and writes it to `output.json`.

Produce the raw string value, without surrounding quotation marks.
