# Unique Values

Given an array that may contain duplicates in `input.json`, write a `jq` program
that removes the duplicate values and writes the result to `output.json`.

The resulting array should also be sorted.
