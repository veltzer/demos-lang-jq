# Default Value

Given an array of objects in `input.json`, write a `jq` program that produces,
for each record, its `nick` if present, otherwise falling back to its `name`.
Write the resulting array to `output.json`.
