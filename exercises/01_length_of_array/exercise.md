# Length of Array

Given an array in `input.json`, write a `jq` program that produces the number
of elements in the array and writes it to `output.json`.

For example, an array of four strings should produce `4`.
