# Extract Nested Value

Given a nested object in `input.json`, write a `jq` program that digs into the
structure to extract the user's city and writes it to `output.json`.

Produce the raw string value, without surrounding quotation marks.
